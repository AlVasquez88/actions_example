name: Hello World
on: [push]
jobs:
  output_hello_world:
    name: Ouoput Hello World
    runs-on: unbunto-latest
    steps:
      -uses: action/checkout@v4
      -name: Run a script
        run: echo "Hello,World!"
